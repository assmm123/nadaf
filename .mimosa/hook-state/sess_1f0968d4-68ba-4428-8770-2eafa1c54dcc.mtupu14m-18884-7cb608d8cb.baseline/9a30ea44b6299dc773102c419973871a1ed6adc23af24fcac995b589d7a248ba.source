@extends('layouts.app')

@section('title', __('checkout.order_created_title'))

@section('content')
    <div class="container-x mt-10 flex justify-center pb-16">
        <div class="card w-full max-w-md p-8 text-center">
            <div class="mx-auto mb-4 flex h-16 w-16 items-center justify-center rounded-full bg-green-100 text-green-600">
                <x-shop-icon name="check" class="h-8 w-8" />
            </div>

            <h1 class="text-xl font-extrabold">{{ __('checkout.order_created_title') }}</h1>

            <p class="mt-3 text-sm leading-6 text-gray-600">
                شكرًا <b>{{ auth()->user()->name }}</b> لثقتك بشركة النداف 💛 — طلبك قيد المراجعة وسيتم الرد عليك في أسرع وقت ممكن.
            </p>

            <p class="mt-5 text-sm text-gray-500">{{ __('checkout.your_code') }}</p>
            <div class="mt-2 rounded-xl border-2 border-dashed border-gold-500 bg-gold-100/50 px-6 py-4">
                <span class="text-3xl font-extrabold tracking-wider text-navy-900" dir="ltr">{{ $order->order_code }}</span>
            </div>
            <p class="mt-3 text-xs text-gray-400">{{ __('checkout.code_note') }}</p>

            <div class="mt-5 space-y-1.5 rounded-lg bg-cream p-4 text-sm">
                <div class="flex justify-between">
                    <span class="text-gray-500">{{ __('cart.grand_total') }}</span>
                    <span class="font-extrabold text-gold-600">{{ fmt_usd($order->total_usd) }}</span>
                </div>
                <p class="text-end text-xs text-gray-400">{{ fmt_syp($order->total_syp) }}</p>
            </div>

            <div class="mt-6 flex flex-col gap-2">
                <a href="{{ route('account.order', $order->order_code) }}" class="btn-primary">{{ __('checkout.view_my_orders') }}</a>
                <a href="{{ route('home') }}" class="btn-outline">{{ __('cart.continue_shopping') }}</a>
            </div>
        </div>
    </div>
@endsection
